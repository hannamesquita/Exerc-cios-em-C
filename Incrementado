#include <stdio.h>

int ehbissexto(int aa){
    if(((aa % 400) == 0) || ((aa % 4 == 0) && (aa % 100) != 0)){
        return 1;
    } else {
    return 0;
    }
}

int diadomes(int mm, int aa){
    switch(mm){
        case 4: case 6: case 9: case 11:
        return 30;
        case 2:
        if(((aa % 400) == 0) || ((aa % 4 == 0) && (aa % 100) != 0)){
            return 29;
        } else {
            return 28;
        }
        default:
        return 31;
    }
}

int numDeBissextos(int)

int main() {
    int dd, mm, aa;

    scanf("%d/%d/%d", &dd, &mm, &aa);

    if(ehbissexto(aa)){
        printf("É bissexto\n");
    } else {
        printf("Não é bissexto\n");
    }

    printf("%d/%d tem %d dias", mm, aa, diadomes(mm, aa));
}
